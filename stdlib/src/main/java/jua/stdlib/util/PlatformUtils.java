package jua.stdlib.util;

/**
 * Утилитарный класс, предоставляющий информацию о платформе и средства взаимодействия с ней.
 */
public class PlatformUtils {

    /**
     * Возвращает {@code true}, если платформа 64-битная, иначе {@code false}.
     *
     * @return {@code true}, если платформа 64-битная, иначе {@code false}.
     */
    public static boolean is64Bit() {
        // todo: Проверка разрядности платформы чересчур комплексная.
        return true;
    }
}
