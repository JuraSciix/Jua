fn bench_stub() {
    var max = 1e9;
    while true {
        var a = ns_time();
        test_stub();
        var b = ns_time();
        var m = (b - a);
        if m < max {
            print_bench_data({
                'name': 'Stub bench',
                'score': m
            });
            max = m;
        }
    }
}

fn test_stub(i = 10) = i <= 0 ? null : test_stub(i - 1);