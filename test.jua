# Комменты, начинающиеся с решетки - отвратительно

# Это тестовый код, чтобы проверить производительность на данном этапе

fn Loop() {
    for i = 0; i < 1_000_000; i++;
}

while true {
    a = time();
    Loop();
    b = time();
    println((b - a) + "ms.");
}